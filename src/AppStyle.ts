import { makeStyles } from "@mui/material";

export const useStyles = makeStyles({
    form: {
        display: 'flex',
        alignItems: 'center',
        marginBottom: '16px',
    },
    textField: {
        marginRight: '16px',
        flexGrow: 1,
    },
    addButton: {
        height: '56px',
    },
}); 